import { StyleSheet } from 'react-native';
import { colors } from '../../styles';

export default StyleSheet.create({
  indicator: {
    color: colors.primary
  },
  indicatorContainer: { flex: 1, justifyContent: 'center', alignContent: 'center' },

});
